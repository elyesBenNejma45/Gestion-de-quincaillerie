{% extends 'base.html.twig' %}
{% block title "Edit counter"%} 

{% block body %}
    <div class="container mt-4">
    <h1> Edit Counter </h1>
    <div class="col-xs-4">
        {{ form_start(form) }}
            {{ form_widget(form.countNum,{'attr':{'type':'text','class':'form-control','placeholder':'count number...'}}) }}
            <br>
            {{ form_widget(form.Edit,{'attr':{'type':'text','class':'btn btn-primary','placeholder':'Submit...'}}) }}        </center>
        {{ form_end(form) }}
    </div>
</div>   
{% endblock %}